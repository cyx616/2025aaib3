# week01-2.py LeetCode 學習計畫 Python 版本2 for迴圈
# 28. Find the Index of the First Occurrence in a String
class Solution:
    def strStr(self, haystack: str, needle: str) -> int:
        return haystack.find(needle)#只有一行
